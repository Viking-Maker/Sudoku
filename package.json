{
  "name": "Sudoku", // Or your repo name
  "version": "0.1.0",
  "private": true,
  "homepage": "https://Viking-Maker.github.io/Sudoku", // <-- ADD/EDIT THIS LINE
  "dependencies": {
    "react": "^18.2.0", // Use versions compatible with your code
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1" // Crucial for CRA build script
    // Add any other dependencies your App.js uses here
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build", // <-- IMPORTANT BUILD SCRIPT
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
